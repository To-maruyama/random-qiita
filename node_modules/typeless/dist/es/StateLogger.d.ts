import { Store } from './Store';
import { ActionLike } from './types';
export declare class StateLogger {
    private stores;
    private prevState;
    private nextState;
    private start;
    private end;
    constructor(stores: Array<Store<any>>);
    setState(type: 'prevState' | 'nextState', state: any): void;
    log(action: ActionLike): void;
}
//# sourceMappingURL=StateLogger.d.ts.map