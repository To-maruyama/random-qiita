import { Subject, Observable } from 'rxjs';
import { Action } from './types';
import { Store } from './Store';
export declare function createOutputStream(action$: Subject<Action>, stores: Store[]): Observable<Action>;
//# sourceMappingURL=createOutputStream.d.ts.map